This folder will contain all the compiled front-end assets that you generate with this Webpack template.

Ideally, the /dist/ folder and relative bundles, should be ignored in your .gitignore file.
Especially if you use git to let your files being version controlled and you have a continuous deployment in place.
